[install]
description = "Install all dependencies"
depends = ["install:*"]

["install:bundle"]
description = "Install Ruby gems via bundler"
run = "bundle install"

["install:pnpm"]
description = "Install JavaScript dependencies via pnpm"
run = "pnpm install"
