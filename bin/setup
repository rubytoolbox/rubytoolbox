#!/usr/bin/env ruby
# frozen_string_literal: true

# Legacy entrypoint — setup now lives in the standard mise task (see tasks/setup.toml)
Dir.chdir(File.expand_path("..", __dir__)) { exec "mise", "run", "setup" }
